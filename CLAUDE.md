# Katha AI repository context

- The approved and active client is `expo/`.
- Read `expo/CLAUDE.md`, `expo/DESIGN.md`, and `expo/BUILD_LOG.md` before changing product UI or onboarding.
- Run Expo commands from `expo/`.
- Treat `ios-katha-ai-create-stories/` and `android-katha-ai/` as preserved Rork reference implementations unless a task explicitly targets native code.
- The backend and Supabase migrations live in the separate `praz-builds/katha-ai-backend` repository.
- Do not reintroduce migration handoff files, duplicate image directories, alternate wordmarks, or parallel design-system documents.
