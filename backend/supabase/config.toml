[project]
id = "iafeuxgoiknncgyjmugd"

[api]
port = 54321
schemas = ["public", "storage"]

[db]
port = 54322

[studio]
port = 54323

[auth]
site_url = "http://localhost:3000"

[auth.external.google]
enabled = false

[auth.external.apple]
enabled = false

[storage]
file_size_limit = "10MB"

[functions.revenuecat-webhook]
verify_jwt = false

[functions.refresh-subscription-grants]
verify_jwt = false

# The store reviewer has no session to present: that is the whole point of the
# function. Every other protection it needs -- one fixed address, a peppered
# HMAC, a constant-time compare, an identical 401 for every failure, and a
# per-email/per-IP lockout -- is inside the function itself.
[functions.reviewer-signin]
verify_jwt = false
