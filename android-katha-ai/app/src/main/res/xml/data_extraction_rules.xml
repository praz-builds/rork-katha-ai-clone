<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12 (API 31) and above.

  Katha AI stores the reader's session token, story drafts and streak state in
  app-private storage. None of that should leave the device through Google's
  cloud backup or through a device-to-device transfer, so both channels exclude
  every domain rather than opting individual files out.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
